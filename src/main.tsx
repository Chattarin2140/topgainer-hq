import React from 'react'
import ReactDOM from 'react-dom/client'
import PnLTracker from './PnLTracker.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PnLTracker />
  </React.StrictMode>,
)
